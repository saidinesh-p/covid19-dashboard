pipeline {
    agent any
    tools { 
        maven 'Apache Maven 3.6.0' 
        jdk '11.0.6"' 
    }
    stages {
        stage ('Initialize') {
            steps {
                sh 'mvn clean'
            }
        }

        stage ('Build') {
            steps {
                 sh 'mvn install'
            }
        }
    }
}
